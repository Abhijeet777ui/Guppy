{
  "name": "@guppy/event-store",
  "version": "1.0.0",
  "description": "Append-only event store with snapshots and replay",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run --passWithNoTests --testTimeout=5000",
    "lint": "biome check ."
  },
  "dependencies": {
    "@guppy/contracts": "workspace:*",
    "@msgpack/msgpack": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "@biomejs/biome": "^1.8.0",
    "@types/node": "^22.0.0"
  },
  "type": "module"
}